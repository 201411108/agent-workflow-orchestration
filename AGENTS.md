# Repository Codex Instructions

## 역할 기반 오케스트레이션

- 신규 기능, 복합 요청, UI/UX 변경은 `$feature-orchestrator`를 우선 사용한다.
- 오케스트레이터가 선택한 `planner`, `designer`, `developer` 프로젝트 custom agent에
  필요한 역할만 위임한다.
- 역할 간에는 전체 대화 대신 목표, 확인된 사실, 결정 사항, 가정, 리스크,
  미결정 사항, 미검증 항목, 다음 역할 입력을 압축해 전달한다.
- 단순 질의, 파일 읽기, 사소한 단일 수정에는 불필요한 위임을 하지 않는다.
- 서브에이전트가 작성하거나 제안한 내용은 메인 에이전트가 검토·통합하고 실제
  변경과 검증 결과를 확인한다.
- 모든 활성 역할이 완료될 때까지 기다린 뒤 메인 에이전트가 최종 결과를 작성한다.
- 서브에이전트 또는 custom agent를 사용할 수 없으면 성공한 것처럼 대체하지 말고
  `degraded` 상태와 누락되는 검토 및 제한을 알린다.

## 저장소 작업 규칙

- 기존 SSoT와 적용되는 저장소 문서를 역할 산출물보다 우선한다. 이 저장소의 역할
  계약을 바꿀 때는 `agent-workflow.manifest.json`과 관련 문서의 일관성을 확인한다.
- `.agent-workflow/state.json`, `current-work.md` 같은 수동 상태 파일을 SSoT로
  간주하지 않는다. 역할 인계, 최종 응답, 기존 SSoT 문서에 결정과 결과를 기록한다.
- 현재 체크아웃된 작업 브랜치를 그대로 사용한다. 오케스트레이션을 이유로 새
  브랜치를 만들거나 다른 브랜치로 전환하지 않는다.
- 기존 미커밋 변경은 사용자 작업으로 취급해 보존한다. stash, reset, restore,
  checkout, 삭제 또는 덮어쓰기를 하지 않는다.
- 커밋할 때는 현재 작업 범위의 파일만 경로를 명시해 stage하고 기존 사용자 변경을
  포함하지 않는다.
- 검증은 실제 실행한 명령과 결과만 보고하며, 실행하지 못한 검사는 미검증으로
  명시한다.
